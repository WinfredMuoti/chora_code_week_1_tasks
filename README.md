# Chora Week 1 — CLI Task Manager
 
A small task manager built test-first as Day 4 of Chora Code Academy's Python track.
 
## Setup
```
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pytest -v
```